server:
  port: 8080

spring:
  application:
    name: company-service
  datasource:
    url: jdbc:mysql://example-1.cxbtixwquuos.ap-northeast-2.rds.amazonaws.com:3306/test
    username: admin
    password: q1w2e3r4
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update #create -> update
    show-sql: true
    properties:
      hibernate:
        format_sql:true

eureka:
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka

management:
  endpoint:
    web:
      exposure:
        include: "*"
